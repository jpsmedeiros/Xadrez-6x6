using System;
using StateNS;

namespace AINS
{
    class AI
    {
        int playerId;
        int type;
        public AI(int playerId, int type){
            this.playerId = playerId;
            this.type = type;
        }

        // public int[] alpha_beta_search(State state){
        //     int v;
        //     v = max_value(state,Int32.MaxValue,Int32.MinValue);

        // }

    }
}
